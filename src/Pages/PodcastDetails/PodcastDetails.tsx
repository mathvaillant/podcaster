import React from 'react'

// Styles
import useStyles from "./styles";

export default function PodcastDetails() {
const { classes } = useStyles();

return <div className={classes.root}>PodcastDetails</div>
}

